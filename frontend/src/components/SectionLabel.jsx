import React from "react"; export default function SectionLabel() {
  return (
    <div className={`flex items-center gap-3 font-mono-tag text-[11px] uppercase tracking-[0.22em] text-orange-600 font-semibold ${className}`}>
        <span className="h-px w-8 bg-orange-600/60" />
        <span>
        {children}</span>
        </div>
      ); }